jwt:
  secret: 'jwt-secret'

spring:
  datasource:
    url: jdbc:postgresql://localhost/spring_user_management_application
    username: postgres
    password: password
  jpa:
    show-sql : false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
